std:import {c++} = workspace:

c++:module {
    files = c++:all-files:
    headers = c++:header-paths {
        addition = all
    }
    uses = {
        export = [ergo add3]
        private = [ergo add1]
    }
}

c++ = workspace:c++

c++:module {
    files = c++:all-files()
    headers = c++:header-paths {
        addition = all
    }
    uses = {
        export = [load add3]
        private = [load add1]
    }
}
